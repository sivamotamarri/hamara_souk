<%= form_for [@commentable, @comment] do |f| %>
  <%#= f.error_messages %>
<p><%=f.hidden_field :user_id , :value => current_user.id %></p>
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </p>
  <p><%= f.submit "Submit" , :id => "comment_submit" %></p>
<% end %>
